import React from 'react'

const page = () => {
  return (
    <div>
      testing 1, add a future like installing setModels 2, a future like when a
      user use first time make sure it has that base model to run first search
    </div>
  )
}

export default page
